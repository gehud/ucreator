[package]
name = "uengine_macro"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
toml_edit = { version = "0.22.7", default-features = false, features = [
  "parse",
] }
syn = "2.0"
quote = "1.0"
proc-macro2 = "1.0"
